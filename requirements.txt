# MCP Tutorial Requirements
#
# These are the dependencies needed to run the notebooks locally.
# If you run Notebook 3 on Google Colab, the Colab-specific packages
# (transformers, torch, accelerate, bitsandbytes) are installed
# inline by the notebook itself, so they are not listed here.

# MCP SDK
mcp==1.27.0

# OpenAI API (Notebooks 1 and 2)
openai==2.30.0

# Jupyter compatibility - allows nested event loops
nest-asyncio==1.6.0

# Type hints support
typing-extensions==4.13.2

# HTTP client (used by Notebook 3 to call the local Ollama API)
requests
