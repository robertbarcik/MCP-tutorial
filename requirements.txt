# MCP course: one requirements file for a local setup (Python 3.10 or newer; tested on 3.12).
#
# Google Colab users do NOT need this: the notebook installs the same pins in its first cell.
#
#   python3 -m venv .venv
#   source .venv/bin/activate        # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#   export OPENAI_API_KEY="sk-..."
#
# Pinned to the exact versions the notebook was executed with (September 2026).
mcp==2.2.0          # Model Context Protocol SDK, 2.x line (spec 2026-07-28)
openai==3.13.0      # OpenAI SDK, Responses API

# Only for running the notebook locally (Colab has this preinstalled):
jupyter
